{
  "Machine": [
    {% for node in groups.compute -%}
    {
      "host_name" : "{{ node }}",
      "location" : "/DataCenter1/Rack1/",
      "frequency" : {{ hostvars[node].ohai_cpu['0'].mhz }},
      "IP" : "{{ hostvars[node].ansible_all_ipv4_addresses[0] }}",
      "Cost": { 
        "Cores": 10,
        "Memory": 2,
        "Disk": 12
      }
    }{% if not loop.last %},{% endif %}
    {%- endfor %}
  ]
}
